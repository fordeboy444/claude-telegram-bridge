# chatmemberowner

- **URL:** https://core.telegram.org/bots/api/available-types/chatmemberowner
- **Summary:** that owns the chat and has all administrator privileges. | status | String | The member's status in the chat, always “creator” | | is_anonymous | Boolean | _True_, if the user's presence in the chat is hidden | | custom_title | String | _Optional_.

# chatmemberowner

ChatMemberOwner

Represents a [chat member](https://core.telegram.org/bots/api#chatmember)
 that owns the chat and has all administrator privileges.

| Field | Type | Description |
| --- | --- | --- |
| status | String | The member's status in the chat, always “creator” |
| user | [User](https://core.telegram.org/bots/api#user) | Information about the user |
| is_anonymous | Boolean | _True_, if the user's presence in the chat is hidden |
| custom_title | String | _Optional_. Custom title for this user |
